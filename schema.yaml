variables:

  create:
    type: boolean
    title: "Create Load Balancer"

  name:
    type: string
    title: "Load Balancer Name"

  lb_type:
    type: enum
    enum:
      - flexible
      - 100Mbps

  existing_id:
    type: string

  backends:
    type: list(string)
